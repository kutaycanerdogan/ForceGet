export enum Mode {
  LCL,
  FCL,
  Air,
}

export enum MovementType {
  DoorToDoor,
  PortToDoor,
  DoorToPort,
  PortToPort,
}

export enum Incoterm {
  DDP,
  DAT,
}

export enum PackageType {
  Pallets,
  Boxes,
  Cartons,
}
export enum CurrencyType {
  USD,
  CNY,
  TRY,
}
